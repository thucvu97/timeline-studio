/**
 * Test providers for Timeline
 */

import { TimelineProviders } from "@/test/test-utils"

// Re-export TimelineProviders as MockTimelineProvider for backward compatibility
export const MockTimelineProvider = TimelineProviders
