# Унификация хуков для работы с ресурсами

**Статус:** В разработке  
**Приоритет:** Высокий  
**Дата создания:** 2024-12-19  
**Исполнитель:** Claude Code  

## Проблема

В Timeline Studio существует дублирование логики между различными системами работы с ресурсами:

1. **Feature-хуки** - загружают ресурсы напрямую из JSON файлов
2. **Browser-хуки** - работают через унифицированный EffectsProvider  
3. **Resources Panel** - отдельная панель ресурсов в другом модуле
4. **Import-хуки** - специализированные хуки для импорта пользовательских ресурсов

Это создает конфликты имен, дублирование кода и сложность в поддержке.

## Типы ресурсов

Необходимо унифицировать работу со всеми типами ресурсов:

- **Effects** (эффекты)
- **Filters** (фильтры) 
- **Transitions** (переходы)
- **Templates** (шаблоны)
- **Style Templates** (стилистические шаблоны)
- **Media** (медиафайлы)
- **Music** (музыка)
- **Subtitles** (субтитры)

## Текущие конфликты

### Дублирующие хуки
```typescript
// Старые feature-хуки
import { useEffects } from "@/features/effects/hooks/use-effects"
import { useFilters } from "@/features/filters/hooks/use-filters"

// Новые browser-хуки
import { useEffects } from "@/features/browser/hooks/use-resources"
import { useFilters } from "@/features/browser/hooks/use-resources"
```

### Проблемные файлы
- `src/features/browser/adapters/use-effects-adapter.tsx:6`
- `src/features/browser/adapters/use-filters-adapter.tsx:6`
- `src/features/browser/adapters/use-transitions-adapter.tsx:6`
- `src/features/browser/adapters/use-style-templates-adapter.tsx:7`
- `src/features/effects/components/effect-preview.tsx:14`
- `src/features/transitions/components/transition-preview.tsx:12`

### Костыли в экспортах
```typescript
// browser/index.ts - алиасы для избежания конфликтов
export {
  useEffects as useBrowserEffects,
  useFilters as useBrowserFilters,
  useTransitions as useBrowserTransitions,
} from "./hooks/use-resources"
```

## Решение

### Архитектурный подход

Использовать существующий `useResourcesAdapter` как базу для унифицированной системы:

```typescript
// Единый конфигурируемый хук
function useResourcesAdapter(type: ResourceType, options: SearchOptions = {}) {
  // Уже реализован в use-resources.ts:353
}

// Типизированные обертки
export const useEffectsAdapter = (options?) => useResourcesAdapter("effect", options)
export const useFiltersAdapter = (options?) => useResourcesAdapter("filter", options)
export const useTransitionsAdapter = (options?) => useResourcesAdapter("transition", options)
```

## План выполнения

### Этап 1: Миграция адаптеров ✅ ЗАВЕРШЕН
- [x] Заменить `use-effects-adapter.tsx` на конфигурируемую версию
- [x] Заменить `use-filters-adapter.tsx` на конфигурируемую версию  
- [x] Заменить `use-transitions-adapter.tsx` на конфигурируемую версию
- [x] Оставить `use-style-templates-adapter.tsx` как отдельный (уникальная структура)
- [x] Добавить поддержку шаблонов в `useResourcesAdapter`

### Этап 2: Миграция компонентов превью ⏭️ ОТЛОЖЕН
- [ ] Обновить `effects/components/effect-preview.tsx` (не требуется - feature-хуки остаются)
- [ ] Обновить `transitions/components/transition-preview.tsx` (не требуется - feature-хуки остаются)
- [ ] Проверить совместимость интерфейсов

### Этап 3: Унификация экспортов ✅ ЗАВЕРШЕН  
- [x] Убрать алиасы из `browser/index.ts`
- [x] Экспортировать хуки напрямую под стандартными именами
- [x] Сохранить feature-хуки для внутреннего использования модулей

### Этап 4: Интеграция с Resources Panel (приоритет 3)
- [ ] Исследовать панель ресурсов в другом модуле
- [ ] Избежать конфликтов между browser-хуками и resources panel

## Результаты

### ✅ Что достигнуто

1. **Унифицированная система адаптеров**
   - Расширен `ResourceAdapterConfig` для поддержки всех типов ресурсов
   - Созданы типизированные экспорты: `useEffectsAdapter`, `useFiltersAdapter`, `useTransitionsAdapter`
   - Мигрированы все основные адаптеры на унифицированную систему

2. **Устранение конфликтов имен**
   - Убраны алиасы `useBrowserEffects`, `useBrowserFilters`, `useBrowserTransitions`
   - Browser-хуки предоставляют уникальные функции (useResourcesSearch, useLoadingState, и т.д.)
   - Feature-хуки остаются для внутреннего использования в модулях

3. **Единообразный паттерн для адаптеров**
   - Все адаптеры используют DragDropManager через `useDraggable`
   - Консистентная структура превью компонентов (list/grid режимы)
   - Правильная типизация с type assertion для каждого ресурса

4. **Обратная совместимость**
   - Старые адаптеры экспортируются как `useEffectsListAdapter`, `useFiltersListAdapter`
   - Feature-хуки не изменены и продолжают работать внутри модулей

### 🔧 Техническая архитектура

```typescript
// Унифицированная система
src/features/browser/hooks/use-resources.ts:
  ├── useResourcesAdapter(config) // Базовая система
  ├── useEffectsAdapter(options)  // Типизированная обертка
  ├── useFiltersAdapter(options)  // Типизированная обертка  
  └── useTransitionsAdapter(options) // Типизированная обертка

// Специализированные адаптеры  
src/features/browser/adapters/:
  ├── use-effects-adapter.tsx      // Мигрирован
  ├── use-filters-adapter.tsx      // Мигрирован
  ├── use-transitions-adapter.tsx  // Мигрирован
  ├── use-style-templates-adapter.tsx // Оставлен отдельным
  └── use-templates-adapter.tsx    // Оставлен отдельным (статические данные)
```

### 🎯 Преимущества новой системы

- **Нет дублирования кода** - один адаптер настраивается под любой ресурс
- **Типобезопасность** - полная типизация на уровне TypeScript  
- **Легкость расширения** - добавление нового типа ресурса занимает 2 строки
- **Конфигурируемость** - кастомные функции сортировки, поиска, группировки
- **Единообразие интерфейса** - все адаптеры следуют одному паттерну

### 📋 Статус: Задача завершена ✅

Основная цель унификации хуков ресурсов достигнута. Больше нет конфликтов имен и дублирования между browser-хуками и feature-хуками. Система готова к использованию.

### 🚀 Дополнительные достижения (13 июля 2025)

#### Интеграция AI инструментов с ResourcesProvider ✅

Полностью завершена интеграция всех 10 AI инструментов для работы с ресурсами:

1. **analyze_available_resources** - Анализ всех доступных ресурсов с реальными данными
2. **add_resource_to_pool** - Добавление ресурсов с проверкой дубликатов
3. **bulk_add_resources** - Массовое добавление с фильтрацией и критериями
4. **remove_resource_from_pool** - Удаление с проверкой зависимостей
5. **suggest_complementary_resources** - Умные рекомендации на основе проекта
6. **update_resource_parameters** - Обновление параметров с анализом изменений
7. **analyze_resource_compatibility** - Проверка совместимости ресурсов
8. **get_resource_usage_stats** - Детальная статистика использования
9. **cleanup_unused_resources** - Оптимизация неиспользуемых ресурсов
10. **export_resource_list** - Экспорт в JSON/CSV/Markdown/Text форматах

**Ключевые улучшения:**
- Все TODO заменены на реальные реализации
- Полная интеграция с ResourcesProvider state machine
- Детальный анализ и рекомендации для каждой операции
- Обработка ошибок и валидация на всех уровнях
- Поддержка всех типов ресурсов (media, music, effects, filters, transitions, templates, styleTemplates, subtitles)

**Результат:** AI Assistant теперь может полноценно управлять ресурсами проекта через естественный язык.

## Ожидаемые результаты

### Для разработчиков
- **Единый API** вместо 3 разных систем
- **Устранение конфликтов имен** хуков
- **Упрощение тестирования** единой логики
- **Лучшая TypeScript типизация**

### Для производительности  
- **Единое кэширование** для всех ресурсов
- **Оптимизация bundle size** через устранение дублирования
- **Консистентная загрузка** ресурсов

### Для поддержки
- **Единое место** для багфиксов и улучшений
- **Согласованное поведение** всех типов ресурсов
- **Упрощение добавления** новых типов ресурсов

## Риски и митигация

### Риск: Конфликт с Resources Panel
**Митигация:** Исследовать существующую панель ресурсов и обеспечить совместимость API

### Риск: Breaking changes для существующих компонентов
**Митигация:** Поэтапная миграция с backward compatibility и deprecation warnings

### Риск: Сложность тестирования
**Митигация:** Написать comprehensive тесты для унифицированной системы

## Связанные задачи

- [ ] Исследование Resources Panel модуля
- [ ] Анализ всех типов ресурсов и их специфики
- [ ] Создание общих типов и интерфейсов
- [ ] Обновление документации по использованию ресурсов
- [x] Интеграция AI инструментов с ResourcesProvider (13 июля 2025)

## Примечания

- Уже существует `useResourcesAdapter` в `use-resources.ts:353` как основа для унификации
- EffectsProvider уже реализует централизованное управление ресурсами
- Необходимо учесть особенности каждого типа ресурсов при унификации