# Тихие обновления приложения

## Описание задачи

Реализовать систему автоматических обновлений приложения Timeline Studio, которая будет работать в фоновом режиме без прерывания работы пользователя.

## Цели

1. **Автоматическая проверка обновлений** - периодическая проверка наличия новых версий
2. **Фоновая загрузка** - скачивание обновлений без влияния на производительность
3. **Умная установка** - установка обновлений в подходящий момент
4. **Откат при ошибках** - возможность вернуться к предыдущей версии

## Технические требования

### 1. Проверка обновлений
- Использование Tauri Updater API
- Проверка каждые 4 часа или при запуске приложения
- Возможность ручной проверки через UI
- Уведомления о доступных обновлениях

### 2. Загрузка обновлений
- Фоновая загрузка с низким приоритетом
- Индикация прогресса загрузки
- Пауза загрузки при активной работе (рендеринг, экспорт)
- Возобновление после сбоев

### 3. Установка обновлений
- Автоматическая установка при закрытии приложения
- Опция "Обновить и перезапустить"
- Сохранение состояния перед обновлением
- Восстановление состояния после перезапуска

### 4. Безопасность
- Проверка цифровой подписи обновлений
- Создание резервной копии перед обновлением
- Механизм отката при ошибках
- Логирование процесса обновления

## Пользовательский интерфейс

### Настройки обновлений
```typescript
interface UpdateSettings {
  autoCheck: boolean           // Автоматическая проверка
  autoDownload: boolean        // Автоматическая загрузка
  autoInstall: boolean         // Автоматическая установка
  checkInterval: number        // Интервал проверки (часы)
  notifyOnUpdate: boolean      // Уведомления о обновлениях
}
```

### Компоненты UI
1. **Индикатор в строке состояния**
   - Иконка обновления при наличии новой версии
   - Прогресс загрузки
   - Меню с опциями

2. **Модальное окно обновления**
   - Информация о новой версии
   - Список изменений
   - Кнопки действий

3. **Страница настроек**
   - Переключатели автоматизации
   - История обновлений
   - Канал обновлений (stable/beta)

## Реализация

### 1. Сервис обновлений
```typescript
// src/features/updates/services/update-service.ts
class UpdateService {
  // Проверка обновлений
  async checkForUpdates(): Promise<UpdateInfo | null>
  
  // Загрузка обновления
  async downloadUpdate(
    onProgress: (progress: number) => void
  ): Promise<void>
  
  // Установка обновления
  async installUpdate(): Promise<void>
  
  // Откат обновления
  async rollbackUpdate(): Promise<void>
}
```

### 2. State Machine для управления процессом
```typescript
const updateMachine = createMachine({
  id: 'update',
  initial: 'idle',
  states: {
    idle: {
      on: { CHECK: 'checking' }
    },
    checking: {
      invoke: {
        src: 'checkForUpdates',
        onDone: [
          { target: 'available', cond: 'hasUpdate' },
          { target: 'idle' }
        ],
        onError: 'error'
      }
    },
    available: {
      on: { 
        DOWNLOAD: 'downloading',
        DISMISS: 'idle'
      }
    },
    downloading: {
      invoke: {
        src: 'downloadUpdate',
        onDone: 'ready',
        onError: 'error'
      }
    },
    ready: {
      on: {
        INSTALL: 'installing',
        POSTPONE: 'idle'
      }
    },
    installing: {
      invoke: {
        src: 'installUpdate',
        onDone: 'completed',
        onError: 'rollback'
      }
    },
    rollback: {
      invoke: {
        src: 'rollbackUpdate',
        onDone: 'error'
      }
    },
    completed: {
      type: 'final'
    },
    error: {
      on: { RETRY: 'idle' }
    }
  }
})
```

### 3. Интеграция с Tauri
```rust
// src-tauri/src/updates/mod.rs
#[tauri::command]
async fn check_update(
    app_handle: tauri::AppHandle,
) -> Result<Option<UpdateInfo>, String> {
    // Использование tauri-plugin-updater
}

#[tauri::command]
async fn download_and_install(
    app_handle: tauri::AppHandle,
) -> Result<(), String> {
    // Загрузка и установка
}
```

## План реализации

### Фаза 1: Базовая функциональность (2 недели) ✅ ЗАВЕРШЕНА
- [x] Интеграция tauri-plugin-updater
- [x] Базовый сервис проверки обновлений
- [x] Простой UI для уведомлений
- [x] Ручная установка обновлений

### Фаза 2: Автоматизация (1 неделя) ✅ ЗАВЕРШЕНА
- [x] Фоновая проверка по расписанию
- [x] Автоматическая загрузка
- [x] Умная установка при закрытии
- [x] Настройки автоматизации

### Фаза 3: Продвинутые функции (1 неделя) ⚠️ ЧАСТИЧНО ЗАВЕРШЕНА
- [x] Прогресс загрузки
- [x] Откат при ошибках
- [ ] Каналы обновлений
- [x] История обновлений

### Фаза 4: Оптимизация (1 неделя)
- [ ] Приоритеты загрузки
- [ ] Дельта-обновления
- [ ] Кэширование
- [ ] Метрики и аналитика

## Тестирование

### Сценарии тестирования
1. **Успешное обновление**
   - Проверка → Загрузка → Установка → Перезапуск

2. **Обновление с ошибкой**
   - Сбой загрузки → Повтор
   - Сбой установки → Откат

3. **Прерывание процесса**
   - Закрытие приложения во время загрузки
   - Отмена обновления пользователем

4. **Краевые случаи**
   - Нет интернета
   - Недостаточно места на диске
   - Поврежденный файл обновления

## Метрики успеха

1. **Технические метрики**
   - Успешность обновлений > 95%
   - Время простоя < 30 секунд
   - Размер обновлений (дельта vs полные)

2. **Пользовательские метрики**
   - Процент пользователей на последней версии
   - Количество отказов от обновлений
   - Обратная связь о процессе

## Риски и митигация

1. **Риск: Поломка приложения после обновления**
   - Митигация: Автоматический откат, резервные копии

2. **Риск: Прерывание работы пользователя**
   - Митигация: Умное планирование, уведомления

3. **Риск: Большой размер обновлений**
   - Митигация: Дельта-обновления, сжатие

## Итоги реализации

### ✅ Выполнено (2025-01-28)

**Backend:**
- ✅ Добавлен `tauri-plugin-updater` в зависимости Cargo.toml
- ✅ Настроена конфигурация updater в tauri.conf.json
- ✅ Созданы Tauri команды: `check_for_update`, `download_and_install_update`, `get_current_version`, `is_updater_available`
- ✅ Команды зарегистрированы в `app_builder.rs`

**Frontend:**
- ✅ Реализован `UpdateService` с полным API для управления обновлениями
- ✅ Создана XState машина `updateMachine` для управления состояниями
- ✅ Разработан hook `useUpdateManager` для React компонентов
- ✅ Создан компонент `UpdateNotification` с прогрессом загрузки
- ✅ Реализован `UpdateManager` с анимациями и позиционированием
- ✅ Добавлен `UpdateStatusIndicator` для строки состояния
- ✅ Создана страница `UpdateSettings` с настройками автопроверки

**Функциональность:**
- ✅ Автоматическая проверка обновлений с настраиваемым интервалом
- ✅ Фоновая загрузка с индикацией прогресса
- ✅ Система уведомлений с различными состояниями
- ✅ Управление настройками через UI
- ✅ Интеграция со строкой состояния
- ✅ Обработка ошибок и возможность повтора

### 📁 Структура файлов

```
src/features/updates/
├── services/
│   ├── update-service.ts        # Основной сервис для работы с обновлениями
│   └── update-machine.ts        # XState машина состояний
├── hooks/
│   └── use-update-manager.ts    # React hook для управления обновлениями
├── components/
│   ├── update-notification.tsx   # Компонент уведомления
│   ├── update-manager.tsx       # Менеджер показа уведомлений
│   ├── update-status-indicator.tsx # Индикатор для строки состояния  
│   └── update-settings.tsx      # Страница настроек
└── index.ts                     # Экспорт всех компонентов

src-tauri/src/updates/
└── mod.rs                       # Backend команды для обновлений
```

### 🚀 Готово к использованию

Система готова к интеграции в основное приложение:

1. **Импорт компонентов:**
```typescript
import { UpdateManager, UpdateStatusIndicator } from '@/features/updates'
```

2. **Добавление в приложение:**
```tsx
// В корневой компонент приложения
<UpdateManager position="top-right" />

// В строку состояния
<UpdateStatusIndicator compact />
```

3. **Настройки в пользовательских настройках:**
```tsx
import { UpdateSettings } from '@/features/updates'
// Добавить как отдельную страницу настроек
```

### 📋 Оставшиеся задачи (низкий приоритет)

- [ ] Каналы обновлений (stable/beta)
- [ ] Приоритеты загрузки
- [ ] Дельта-обновления
- [ ] Дополнительное кэширование
- [ ] Расширенная аналитика

## Ссылки

- [Tauri Updater Guide](https://v2.tauri.app/plugin/updater/)
- [Electron Auto Updater](https://www.electronjs.org/docs/latest/api/auto-updater) (для сравнения)
- [Squirrel.Windows](https://github.com/Squirrel/Squirrel.Windows) (альтернативный подход)