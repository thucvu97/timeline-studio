# Мультикамерная система (Multi-cam) - Завершение реализации

## 📋 Обзор

Мультикамерная система Timeline Studio находится на стадии частичной реализации. Основная инфраструктура уже заложена, но требуется завершение реализации UI-компонентов и логики переключения камер.

## 🎯 Текущее состояние

### ✅ Уже реализовано:
- **Типы данных**: 
  - `SequenceType` включает `"multicam"` (`src/features/timeline/types/sequence.ts:15`)
  - `LinkedClipsPair` поддерживает `'multi-camera'` (`src/features/timeline/hooks/use-linked-clips.ts:15`)
- **Система связанных клипов**: 
  - Полный хук `useLinkedClips` с функциями связывания, синхронизации и поиска связей
  - Автоматическое связывание по медиа файлу
  - Синхронизация позиции и длительности связанных клипов
  - **Требует доработки**: определение типа связи (видео/аудио) на основе типа трека (строки 119, 233)
- **Клавиатурные сочетания**: 
  - Определены для переключения камер 1-4 (`src/features/keyboard-shortcuts/presets/timeline-preset.ts:184-208`)
  - Требуется расширение до 9 камер
- **AI инструменты**: 
  - Поддержка категории "multi-camera" в шаблонах (`src/features/ai-chat/tools/template-layout-tools.ts:28`)
- **Локализация**: Подготовлены переводы для мультикамерного монтажа
- **Частичная UI-логика**: Закомментированная функция `handleSwitchCamera` в player-controls

### ❌ Требует реализации:
- **Компонент выбора угла камеры**
- **Превью нескольких камер одновременно**
- **Активация функции переключения камер**
- **Определение типа клипа (видео/аудио) по типу трека**
- **Синхронизация по таймкоду и аудио**
- **Специализированные иконки**

## 🚧 Задачи для завершения

### 1. Доработка системы связанных клипов
- [x] Реализовать определение типа клипа (видео/аудио) на основе типа трека
- [x] Исправить логику определения типа связи в `use-linked-clips.ts` (строки 119, 233)
- [x] Добавить метод фильтрации связанных клипов по типу "multi-camera"

### 2. Завершение UI в player-controls
- [x] Активировать функцию `handleSwitchCamera()` 
- [x] Добавить иконку переключения камер
- [ ] Создать dropdown для выбора камеры
- [x] Добавить индикатор текущей камеры
- [x] Интегрировать с хуком `useLinkedClips`

### 3. Создание angle-viewer компонента
- [x] Компонент для одновременного просмотра всех углов
- [x] Сетка превью камер (использовать связанные клипы типа "multi-camera")
- [x] Выбор активной камеры кликом
- [x] Синхронизация воспроизведения через `syncLinkedClips`

### 4. Хуки для мультикамеры
- [x] `use-multicam.ts` - обертка над `useLinkedClips` для мультикамерной логики
- [ ] `use-camera-sync.ts` - синхронизация углов
- [x] `use-angle-selection.ts` - выбор и переключение

### 5. Синхронизация системы
- [x] Автоматическая синхронизация по аудио (использовать `findPotentialLinks`)
- [x] Ручная синхронизация по меткам
- [x] Синхронизация по таймкоду (метаданные клипов)
- [ ] Система clapperboard detection

### 6. Интеграция с клавиатурой
- [x] Активация существующих сочетаний (1-4)
- [x] Расширение до 9 камер
- [x] Быстрое переключение без прерывания воспроизведения
- [x] Визуальная обратная связь

## 📐 Архитектура реализации

### Компоненты:
```
src/features/multicam/
├── components/
│   ├── angle-viewer.tsx         # Сетка превью камер
│   ├── camera-selector.tsx      # Селектор камеры
│   ├── sync-controls.tsx        # Контролы синхронизации
│   └── multicam-indicator.tsx   # Индикатор в плеере
├── hooks/
│   ├── use-multicam.ts          # Обертка над useLinkedClips
│   ├── use-camera-sync.ts       # Синхронизация
│   └── use-angle-selection.ts   # Выбор угла
├── services/
│   ├── multicam-manager.ts      # Менеджер мультикамеры
│   └── sync-detector.ts         # Детектор синхронизации
└── types/
    └── multicam.ts              # Типы мультикамеры
```

### Интеграция с существующим кодом:
- Использование `useLinkedClips` из `src/features/timeline/hooks/use-linked-clips.ts`
- Расширение `player-controls.tsx` для активации `handleSwitchCamera`
- Интеграция с клавиатурными сочетаниями из `timeline-preset.ts`
- Использование типов `SequenceType` и `LinkedClipsPair`

## 🎨 UI/UX концепция

### Кнопка в плеере:
```
[📹 2/4]  - Показывает текущую камеру и общее количество
```

### Angle Viewer:
```
┌─────────────┬─────────────┐
│  Camera 1   │  Camera 2   │
│  (Active)   │             │
├─────────────┼─────────────┤
│  Camera 3   │  Camera 4   │
│             │             │
└─────────────┴─────────────┘
```

### Dropdown селектор:
```
Camera 1 (Main) ✓
Camera 2 (Wide)
Camera 3 (Close-up)
Camera 4 (B-roll)
```

## 🔧 Технические детали

### Основной хук `use-multicam` (обертка над `useLinkedClips`):
```typescript
interface MulticamState {
  angles: MulticamAngle[]
  activeAngle: number
  isSync: boolean
  syncOffset: number[]
  linkedPairs: LinkedClipsPair[] // из useLinkedClips
}

interface MulticamAngle {
  id: string
  name: string
  clipId: string
  linkedClip: TimelineClip | null // из useLinkedClips
  preview?: string
  syncOffset: number
}

// Использование существующего функционала
const { 
  linkedPairs,
  linkClips,
  syncLinkedClips,
  findPotentialLinks 
} = useLinkedClips()

// Фильтрация только мультикамерных связей
const multicamPairs = linkedPairs.filter(pair => 
  pair.type === 'multi-camera'
)
```

### Синхронизация:
```typescript
interface SyncPoint {
  angleId: string
  timestamp: number
  confidence: number
  method: 'audio' | 'timecode' | 'manual'
}

// Использование существующего метода
const potentialLinks = findPotentialLinks()
// Фильтрация по медиа файлу для мультикамеры
```

## 📊 Приоритеты реализации

### Высокий приоритет:
1. **Доработка `use-linked-clips.ts`** - исправить определение типа связи
2. **Завершение кнопки переключения** - активировать `handleSwitchCamera`
3. **Хук use-multicam** - обертка над `useLinkedClips`
4. **Интеграция с клавиатурой** - активация горячих клавиш 1-4

### Средний приоритет:
5. **Angle viewer** - расширенный просмотр всех углов
6. **Автоматическая синхронизация** - использование `findPotentialLinks`
7. **Расширение горячих клавиш** - поддержка 5-9 камер

### Низкий приоритет:
8. **Продвинутые функции синхронизации** - детекция хлопушки
9. **Цветовая коррекция между камерами** - пост-обработка
10. **AI-ассистент для мультикамеры** - автоматический выбор лучшего угла

## 🎯 Критерии завершения

### Минимальная реализация (MVP):
- [x] Существующая инфраструктура связанных клипов
- [x] Работающая кнопка переключения в плеере
- [x] Базовый хук управления мультикамерой
- [x] Клавиатурные сочетания 1-9

### Полная реализация:
- [x] Angle viewer с сеткой превью
- [ ] Автоматическая синхронизация
- [ ] Продвинутые инструменты синхронизации
- [x] Визуальные индикаторы и обратная связь

## 🔗 Связанные задачи

- **Advanced Timeline Features** - использует систему связанных клипов
- **Keyboard Shortcuts** - активация существующих сочетаний
- **Video Player** - интеграция с контролами плеера

---

*Создано: 2025-01-17*
*Обновлено: 2025-01-28*
*Статус: В разработке*
*Приоритет: Средний*

## 📝 История изменений

### 2025-01-28 (утро)
- Обновлена документация с учетом текущего состояния кода
- Добавлены ссылки на конкретные файлы и строки кода
- Уточнена архитектура с использованием существующего `useLinkedClips`
- Обновлены приоритеты с учетом уже реализованного функционала

### 2025-01-28 (вечер)
- ✅ Реализована полная система определения типа связи клипов на основе типа трека
- ✅ Активирована функция `handleSwitchCamera()` в player-controls
- ✅ Создан полноценный модуль мультикамеры с компонентами, хуками и сервисами
- ✅ Реализован AngleViewer с сеткой превью и поддержкой до 9 камер
- ✅ Интегрированы горячие клавиши 1-9 для переключения камер
- ✅ Добавлена передача baseClipId через selectedClipId из плеера
- ✅ Реализована интеграция с API плеера для переключения видео
- ✅ MVP мультикамерной системы полностью завершен

### 2025-01-28 (ночь)
- ✅ Реализована синхронизация по таймкоду
- ✅ Создан сервис `timecode-sync.ts` для извлечения и сравнения таймкодов
- ✅ Добавлен компонент `sync-controls.tsx` для UI управления синхронизацией
- ✅ Реализована ручная синхронизация с визуальным слайдером
- ✅ Добавлен компонент `sync-info.tsx` для отображения статуса синхронизации
- ✅ Интегрирована синхронизация по времени создания файлов как fallback
- ✅ Написаны тесты для функций синхронизации

### 2025-01-29 (утро)
- ✅ Реализована синхронизация по аудио
- ✅ Создан сервис `audio-sync.ts` с алгоритмом корреляции аудиосигналов
- ✅ Добавлен компонент `audio-sync-dialog.tsx` с визуализацией процесса
- ✅ Реализован анализ аудиодорожек и вычисление смещений
- ✅ Добавлена поддержка прогресса синхронизации
- ✅ Интегрирована оценка качества синхронизации
- ✅ Полная мультикамерная система с синхронизацией завершена!